let n = 0;
while n < 500 do
    let t0 = clock();
    let i, s = 1;
    while i < n do
        let s = s * (i + 1);
        inc i;
    end
    let t1 = clock();
    print "fact(" + n + ") = " + s + ", Time: " + (t1 - t0) + "s.";
    inc n;
end